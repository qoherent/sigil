interface:
  display_name: "Sigil"
  short_description: "Use Sigil specs and CLI before code generation"
  default_prompt: "Use Sigil and its CLI to check, query, improve, and align component specs, then stop for review before generating code."
