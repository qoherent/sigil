component ReleaseFixture {
  goal {
    Exercise the packaged native runtime.
  }
  interface {
    *fixture operation* accepts a deterministic input.

    Use fixture operation with [notes](./notes.md).
    ```text
    fixture operation stays literal here.
    ```
  }
}
